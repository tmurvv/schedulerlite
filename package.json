{
  "name": "schedulerlite",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "format": "prettier \"**/*.{ts,tsx,js,jsx,json,md}\" --write",
    "precom": "pnpm exec prettier --write . && pnpm -C apps/timesheets/ui exec tsc -p tsconfig.app.json --noEmit && pnpm -C apps/timesheets/api exec tsc --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.6.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
